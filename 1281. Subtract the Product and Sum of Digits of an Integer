var subtractProductAndSum = function(n) {
    let reducer = (a, b) => +a + +b;
    let reducers = (a, b) => +a * +b;
    let sum = n.toString().split('').reduce(reducer);
    let product = n.toString().split('').reduce(reducers);
    return product - sum;
};
